<!--
  The window's frame. Our own, because the typography and the colours are our
  own: a system title bar would bring Windows' font and palette onto a panel
  that has its own.

  Rounding the window's corners is left to the system: in Windows 11 the
  window manager draws it, and repeating that by hand means arguing with it
  over half a pixel on every monitor.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework">

  <Style x:Key="Window.Apparatus" TargetType="Window">
    <Setter Property="Background" Value="{DynamicResource C.Face}" />
    <Setter Property="Foreground" Value="{DynamicResource C.Ink}" />
    <Setter Property="FontFamily" Value="{StaticResource Font.Ui}" />
    <Setter Property="MinWidth" Value="720" />
    <Setter Property="MinHeight" Value="480" />
    <Setter Property="shell:WindowChrome.WindowChrome">
      <Setter.Value>
        <!--
          CaptionHeight equals the height of our title bar: the window is
          dragged by it, and a double click on it maximises the window.
          GlassFrameThickness of one leaves the shadow and the window's
          rounding to the system, and ResizeBorder gives the familiar edges
          for resizing.
        -->
        <shell:WindowChrome CaptionHeight="40"
                            CornerRadius="0"
                            GlassFrameThickness="1"
                            ResizeBorderThickness="6"
                            UseAeroCaptionButtons="False" />
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
