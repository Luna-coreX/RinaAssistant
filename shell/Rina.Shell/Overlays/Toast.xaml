<Window x:Class="Rina.Shell.Overlays.Toast"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="380" SizeToContent="Height"
        WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="True"
        Background="Transparent" ShowInTaskbar="False" Topmost="True"
        ShowActivated="False" Focusable="False">
  <Border x:Name="Card" Background="{DynamicResource C.Face}"
          BorderBrush="{DynamicResource C.Seam}" BorderThickness="1"
          CornerRadius="{StaticResource Radius.Tight}"
          Padding="16,14,16,14" Opacity="0">
    <Border.RenderTransform>
      <TranslateTransform x:Name="Slide" Y="12" />
    </Border.RenderTransform>
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <!-- Полоса акцентом слева: знак, что говорит Рина, а не система. -->
      <Border Grid.Column="0" Width="3" CornerRadius="2"
              Background="{DynamicResource C.Signal}"
              Margin="0,0,12,0" />

      <TextBlock x:Name="Body" Grid.Column="1"
                 Style="{StaticResource Text.Body}"
                 TextWrapping="Wrap" MaxHeight="160"
                 TextTrimming="CharacterEllipsis" />
    </Grid>
  </Border>
</Window>
