<!--
  Things to do (4.0b-A13).

  A window of its own rather than a tab: a person opens the list, does
  something to it and goes back to what they were doing. A tab is where one
  lives; a window is where one looks in. The same decision as the lists in
  the settings (4.0b-A10).
-->
<Window x:Class="Rina.Shell.Pages.TodoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:loc="clr-namespace:Rina.Shell.Strings"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{loc:S 'Дела'}" Width="520" Height="560"
        WindowStyle="None" ResizeMode="CanResize"
        AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
  <Border Background="{DynamicResource C.Face}"
          BorderBrush="{DynamicResource C.Seam}" BorderThickness="1"
          CornerRadius="{StaticResource Radius.Max}" Padding="24">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <TextBlock Grid.Row="0" Text="{loc:S 'Дела'}"
                 Style="{StaticResource Text.Title}" Margin="0,0,0,4" />
      <TextBlock Grid.Row="0" x:Name="Count"
                 Style="{StaticResource Text.Meta}"
                 HorizontalAlignment="Right" VerticalAlignment="Center" />

      <!-- Adding is possible here too: somebody who opened the list has
           most often come to put something on it. -->
      <Grid Grid.Row="1" Margin="0,12,0,12">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBox x:Name="Input" Grid.Column="0"
                 Style="{StaticResource Field}"
                 KeyDown="OnInputKey" />
        <Button Grid.Column="1" Margin="8,0,0,0"
                Style="{StaticResource Btn}"
                Content="{loc:S 'Записать'}" Click="OnAdd" />
      </Grid>

      <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
        <StackPanel x:Name="Items" />
      </ScrollViewer>

      <ContentControl Grid.Row="2" x:Name="Empty" Visibility="Collapsed" />

      <StackPanel Grid.Row="3" Orientation="Horizontal"
                  HorizontalAlignment="Right" Margin="0,16,0,0">
        <CheckBox x:Name="ShowDone" Style="{StaticResource Toggle}"
                  Content="{loc:S 'Показывать сделанные'}"
                  VerticalAlignment="Center" Margin="0,0,16,0"
                  Click="OnToggleDone" />
        <Button Style="{StaticResource Btn}" Content="{loc:S 'Закрыть'}"
                Click="OnClose" IsCancel="True" />
      </StackPanel>
    </Grid>
  </Border>
</Window>
