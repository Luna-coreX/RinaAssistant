<Window x:Class="Rina.Shell.FloatingBar"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:Rina.Shell.Strings"
        Width="520" SizeToContent="Height"
        WindowStyle="None" ResizeMode="NoResize" Topmost="True"
        ShowInTaskbar="False" AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="Manual">
  <Border Background="{DynamicResource C.Face}"
          BorderBrush="{DynamicResource C.Seam}" BorderThickness="1"
          CornerRadius="{StaticResource Radius.Max}"
          Padding="6" MouseLeftButtonDown="OnDrag">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <TextBox x:Name="Input" Grid.Column="0"
               Style="{StaticResource Field}"
               KeyDown="OnKey" />

      <TextBlock x:Name="State" Grid.Column="1"
                 Style="{StaticResource Text.Meta}"
                 VerticalAlignment="Center" Margin="10,0,8,0"
                 Text="{loc:S 'Enter — отправить, Esc — скрыть'}" />
    </Grid>
  </Border>
</Window>
