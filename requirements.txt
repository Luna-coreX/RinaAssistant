PySide6>=6.6

# --- Опционально: глобальная горячая клавиша ---
pynput>=1.7

# --- Аудио-ввод (микрофон) без PyAudio ---
sounddevice>=0.4
numpy>=1.24
soundfile>=0.12        # для воспроизведения аудио (TTS)

# ============ TTS (озвучка) — любой на выбор ============
pyttsx3>=2.98          # офлайн, системный
edge-tts>=6.1          # онлайн, нейросетевые голоса (рекомендуется)
# gTTS>=2.5            # онлайн, Google
# piper-tts            # офлайн нейро (нужна .onnx-модель)

# ============ STT (распознавание) — любой на выбор ============
SpeechRecognition>=3.10   # для Google и Sphinx
# vosk>=0.3.45         # офлайн (нужна модель)
# openai-whisper       # офлайн, точный (скачивает модель сам)
# pocketsphinx         # офлайн, лёгкий

# --- выбор аудио-устройств и тест микрофона ---
# (уже покрыто sounddevice + numpy выше)
